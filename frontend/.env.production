# Intentionally left without fixed production values.
# Runtime config via /config.js is the preferred source in containers.
#
# Optional build-time fallback values (uncomment if you explicitly need them):
# VITE_API_BASE_URL=
# VITE_KEYCLOAK_URL=
# VITE_KEYCLOAK_REALM=
# VITE_KEYCLOAK_CLIENT_ID=
